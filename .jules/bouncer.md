- Enforced minimum horizontal separation between path segments in the orthogonal edge routing algorithm. Discovered and fixed an issue where overlapping staggered source points caused segments mapping to identical targets to lack horizontal clearance. Also fixed the horizontal-routing test to simulate edge layout boundary behavior accurately by removing artificial overlaps in test variables.
