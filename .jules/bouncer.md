Restored `horizontal-routing-boundary.test.ts` by fixing invalid test inputs. Edges out of a parent node have their source points staggered horizontally in the real application, so tests must mimic this or else A* pathfinding naturally traces identical paths resulting in artificial test failures.
