- Fixed the disabled 'Horizontal Alignment Non-Overlap Property' test in test/layout/horizontal-routing-boundary.test.ts. The original test erroneously used a fixed source point which caused paths to overlap, misrepresenting the runtime layout behavior. Modified the test to explicitly stagger source points to accurately emulate layout execution context, satisfying the non-overlap behavior specification.
