--- test/renderer/renderer.test.ts
+++ test/renderer/renderer.test.ts
@@ -246,7 +246,7 @@
     });

     // Toggle search open
-    const searchToggleBtn = container.querySelector('button[title="Toggle Search"]') as HTMLButtonElement;
+    const searchToggleBtn = container.querySelector('button[title="Toggle Search (/)"]') as HTMLButtonElement;
     expect(searchToggleBtn).not.toBeNull();
     searchToggleBtn.click();
